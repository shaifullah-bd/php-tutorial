<?php 

/**
 * We are learn php basic syntax
 */

declare(strict_types=1);

namespace App\Controllers;

/**
 * We are learn about comments, variable, echo, print, data types, string, numbers, math, constants, operators, if-else, switch, loops, function, arrays, superglobals
 */
class Tutorial1Class {
    
  public function printMsg(string $msgData = null):string
  {
    return $msgData;
  }
  
}